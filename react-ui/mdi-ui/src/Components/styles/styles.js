import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      maxWidth: 500,
    },
    media: {
      height: 300,
    },
  });

export function cardWidth(){
      return 500;
  }